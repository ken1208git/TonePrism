# 部員向けマニュアル docs サイト (MkDocs Material) のビルド依存。
# CI (.github/workflows/docs.yml) とローカルプレビュー (mkdocs serve) で共用。
# メジャー固定で再現性を確保。
mkdocs-material>=9,<10
mkdocs-glightbox>=0.4,<1
mkdocs-git-revision-date-localized-plugin>=1.2,<2
